console.log("Start");

/* function findEvenNumbers(start, end) {
    if (start > end) {
        return [];
    }
    if (start % 2 !== 0) {
        start++;
    }
    console.log(start); // Her adımda sayıyı konsola yazdır
    return [start, ...findEvenNumbers(start + 2, end)];
}

// Örnek kullanım
let evenNumbers = findEvenNumbers(1, 10);
console.log("Even Numbers:", evenNumbers); */
dwaawddawd
console.log("End");
